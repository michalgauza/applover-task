name: login_app
description: applovers login task

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^3.0.10
  google_fonts: ^1.1.2
  flutter_svg: ^0.19.3
  flutter_bloc: ^6.1.3
  get_it: ^6.0.0
  flutter_native_splash: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - assets/
